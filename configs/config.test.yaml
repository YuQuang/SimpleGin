############################
#  這是測試用的環境設定        #
############################

# Server config
server.port: 3000
server.host: "0.0.0.0"
server.mode: development

# Database config
db.port: 5433
db.host: localhost
db.user: postgres
db.password: mysecretpassword
db.name: postgres

# Swagger config
swagger.enabled: true
swagger.host: "<public_ip>"